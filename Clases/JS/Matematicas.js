// genera un numero aleatorio entre 0 y 1.
Math.random();

// genera un numero aleatorio entre 0 y 10 y este se redondea con el math floor
Math.floor(Math.random() * 10);

// parseInt convierte un string en un numero.
var num = "42";
console.log(parseInt(num));

// parseInt convierte un numero binario en un numero decimal.
var numeroBinario = "101010";
console.log(parseInt(numeroBinario, 2));


// Ejemplos